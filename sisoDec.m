function [Lu_out] = sisoDec(Lu_in, Lc_in, trellis)


% setup
Infty = 1e10;
nStates = trellis.numStates;
%----------------

% 前向递推计算alpha
lastState = 1;
for k=2:N+1
    last
    for state = 1:nStates
        M = -Infty*ones(1,nStates);
        
        M(
end

